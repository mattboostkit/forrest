import React from 'react';
import ReactDOM from 'react-dom/client';
import { HelmetProvider } from 'react-helmet-async';
import App from './App';
import './index.css';

// Make sure environment variables are loaded
import.meta.env.VITE_GA_MEASUREMENT_ID || console.warn('Google Analytics Measurement ID not found in environment variables');

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HelmetProvider>
      <App />
    </HelmetProvider>
  </React.StrictMode>
);
